
        # grafo.append(Vertice(4, 10))
        # grafo.append(Vertice(9, 8))
        # grafo.append(Vertice(1, 5))
        # grafo.append(Vertice(6, 5))
        # grafo.append(Vertice(7, 3))
        # grafo.append(Vertice(0, 1))
        # grafo.append(Vertice(5, 2))
        # grafo.append(Vertice(8, 3))
        # grafo.append(Vertice(7, 9))
        # grafo.append(Vertice(6, 9))


                # if(len(vertices_proximos) < 3):
                #     novo_vertice = (j, distancia)

                #     # Insere de forma ordenada
                #     for k in range(0, len(vertices_proximos)+1):
                #         if(vertices_proximos[k][1] > distancia or k == len(vertices_proximos)):
                #             vertices_proximos.insert(k, novo_vertice)
                #             break

                #     vertice_mais_distante = vertices_proximos[-1]

                # elif(vertice_mais_distante[1] > distancia):
                #     novo_vertice = (j, distancia)

                #     vertices_proximos.remove(vertice_mais_distante)

                #     # Insere de forma ordenada
                #     for k in range(0, len(vertices_proximos)+1):
                #         if(vertices_proximos[k][1] > distancia or k == len(vertices_proximos)):
                #             vertices_proximos.insert(k, novo_vertice)
                #             break

                #     vertice_mais_distante = vertices_proximos[-1]

            # grafo[i].adiciona_aresta(j, distancia)
            # grafo[j].adiciona_aresta(i, distancia)